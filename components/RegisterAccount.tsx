import { StyleSheet, View, Text } from 'react-native'

const RegisterAccountScreen = () => {
	return (
		<View>
			<Text>RegisterAccountScreen</Text>
		</View>
	)
}

export default RegisterAccountScreen

const styles = StyleSheet.create({})
